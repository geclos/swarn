Add support for an optional --name and --excited CLI flag in the greeting app.

Requirements:
- Default name remains "world" when --name is not provided.
- --excited should uppercase the greeting and add "!!!" at the end.
- Keep the base format "Hello, <name>" for non-excited output.
- Update the README usage examples to document the new flags.
- Keep the code small and idiomatic; follow existing style.
